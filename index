<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ictBook | Year 5–7 ICT Reviewer</title>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<style>
    :root {
        --meta-blue: #1877F2;
        --meta-blue-dark: #166fe0;
        --bg-gray: #f0f2f5;
        --text-main: #050505;
        --text-secondary: #65676b;
        --card-shadow: 0 1px 2px rgba(0,0,0,.1);
        --green: #31a24c;
        --red: #e41e3f;
        --amber: #f7b928;
        --purple: #8a3ffc;
    }
    * { margin:0; padding:0; box-sizing:border-box; }
    body {
        font-family: 'Segoe UI', Helvetica, Arial, sans-serif;
        background: var(--bg-gray);
        color: var(--text-main);
        overflow-x: hidden;
    }

    /* NAV */
    nav {
        background:#fff; height:56px; display:flex; align-items:center;
        justify-content:space-between; padding:0 16px; box-shadow:var(--card-shadow);
        position:sticky; top:0; z-index:1000;
    }
    .nav-left { display:flex; align-items:center; gap:8px; }
    .nav-left .logo { color:var(--meta-blue); font-size:38px; }
    .brand-name { font-weight:700; font-size:20px; color:var(--meta-blue); letter-spacing:-.5px; }
    .search-bar {
        background:var(--bg-gray); padding:9px 12px; border-radius:20px;
        display:flex; align-items:center; gap:8px; max-width:240px;
    }
    .search-bar input { border:none; background:transparent; outline:none; font-size:14px; width:100%; }
    .nav-right { display:flex; align-items:center; gap:14px; }
    .level-badge {
        background:var(--bg-gray); border-radius:20px; padding:6px 10px; display:flex;
        align-items:center; gap:6px; font-size:13px; font-weight:600; color:var(--text-secondary);
    }
    .level-badge select { border:none; background:transparent; font-weight:700; color:var(--meta-blue); outline:none; cursor:pointer; font-size:13px; }

    /* LAYOUT */
    .container {
        display:grid; grid-template-columns:280px 1fr 320px; gap:16px;
        max-width:1200px; margin:16px auto; padding:0 16px; align-items:start;
    }
    .sidebar, .widgets { position:sticky; top:72px; }

    /* SIDEBAR */
    .sidebar h3 { padding:8px 12px; font-size:17px; color:var(--text-secondary); }
    .sidebar-item {
        padding:10px 12px; border-radius:8px; display:flex; align-items:center; gap:12px;
        cursor:pointer; transition:.15s; margin-bottom:2px; font-weight:600; font-size:15px;
    }
    .sidebar-item:hover { background:#e4e6e9; }
    .sidebar-item i { width:24px; font-size:20px; text-align:center; }
    .active-tab { background:#e7f3ff; color:var(--meta-blue); }
    .active-tab:hover { background:#daeafe; }
    .sidebar-divider { height:1px; background:#ced0d4; margin:10px 8px; }
    .pillar-count { margin-left:auto; font-size:12px; background:#e4e6e9; color:var(--text-secondary);
        border-radius:10px; padding:1px 8px; font-weight:700; }

    /* MODE SWITCHER */
    .mode-switch {
        background:#fff; border-radius:10px; box-shadow:var(--card-shadow);
        padding:6px; display:flex; gap:4px; margin-bottom:16px;
    }
    .mode-btn {
        flex:1; padding:10px 6px; border:none; background:transparent; border-radius:8px;
        cursor:pointer; font-weight:700; font-size:14px; color:var(--text-secondary);
        display:flex; align-items:center; justify-content:center; gap:6px; transition:.15s;
    }
    .mode-btn.active { background:var(--meta-blue); color:#fff; }
    .mode-btn:not(.active):hover { background:var(--bg-gray); }

    /* STORIES */
    .stories-container { display:flex; gap:8px; margin-bottom:16px; overflow-x:auto; padding-bottom:6px; }
    .story-card {
        min-width:110px; height:150px; background:#fff; border-radius:12px; position:relative;
        cursor:pointer; box-shadow:var(--card-shadow); overflow:hidden; border:1px solid #ddd;
        transition:transform .15s;
    }
    .story-card:hover { transform:translateY(-3px); }
    .story-card .fact-icon { height:65%; display:flex; align-items:center; justify-content:center; color:#fff; font-size:28px; }
    .story-name { padding:8px; font-size:12px; font-weight:700; text-align:center; line-height:1.3; }

    /* POSTS */
    .post {
        background:#fff; border-radius:10px; padding:14px; box-shadow:var(--card-shadow); margin-bottom:16px;
    }
    .post-header { display:flex; align-items:center; gap:10px; margin-bottom:12px; }
    .post-avatar {
        width:42px; height:42px; border-radius:50%; color:#fff; flex-shrink:0;
        display:flex; align-items:center; justify-content:center; font-weight:700; font-size:18px;
    }
    .post-info b { font-size:15px; display:block; }
    .post-info small { color:var(--text-secondary); font-size:13px; display:flex; align-items:center; gap:5px; }
    .yr-tag { font-size:11px; font-weight:700; padding:1px 7px; border-radius:10px; color:#fff; }
    .post-content { font-size:15px; line-height:1.5; margin-bottom:12px; }
    .post-content b { color:var(--text-main); }
    .post-image {
        width:100%; background:linear-gradient(135deg,#f5f7fa,#e9edf2); border-radius:8px;
        padding:22px; text-align:center; font-family:'Courier New',monospace; font-size:15px;
        border:1px solid #e4e6e9; margin-bottom:12px; font-weight:700; color:#1a1a1a;
    }
    .post-actions { border-top:1px solid #e4e6e9; border-bottom:1px solid #e4e6e9; display:flex; padding:4px 0; margin-bottom:8px; }
    .action-btn { flex:1; padding:9px; text-align:center; border-radius:6px; cursor:pointer; color:var(--text-secondary); font-weight:600; font-size:14px; user-select:none; }
    .action-btn:hover { background:var(--bg-gray); }
    .action-btn.liked { color:var(--meta-blue); }
    .action-btn.saved { color:var(--amber); }
    .comment-section { margin-top:8px; }
    .comment { background:var(--bg-gray); padding:8px 12px; border-radius:15px; margin-bottom:5px; font-size:13px; line-height:1.4; }
    .comment-input { display:flex; gap:8px; margin-top:10px; align-items:center; }
    .comment-input input { flex:1; background:var(--bg-gray); border:none; border-radius:20px; padding:9px 14px; outline:none; }

    /* FLASHCARD MODE */
    .flashcard-wrap { perspective:1200px; margin-bottom:16px; }
    .flashcard {
        background:#fff; border-radius:14px; box-shadow:0 2px 12px rgba(0,0,0,.12);
        min-height:300px; cursor:pointer; transition:transform .55s; transform-style:preserve-3d;
        position:relative;
    }
    .flashcard.flipped { transform:rotateY(180deg); }
    .card-face {
        position:absolute; inset:0; backface-visibility:hidden; padding:30px;
        display:flex; flex-direction:column; justify-content:center; align-items:center; text-align:center;
    }
    .card-back { transform:rotateY(180deg); background:#e7f3ff; border-radius:14px; }
    .card-eyebrow { font-size:12px; font-weight:700; text-transform:uppercase; letter-spacing:1px; color:var(--meta-blue); margin-bottom:14px; }
    .card-q { font-size:24px; font-weight:700; line-height:1.3; }
    .card-a { font-size:18px; line-height:1.5; }
    .card-hint { position:absolute; bottom:14px; font-size:12px; color:var(--text-secondary); }
    .flash-controls { display:flex; gap:10px; align-items:center; justify-content:space-between; }
    .flash-controls button {
        background:#fff; border:none; box-shadow:var(--card-shadow); border-radius:10px;
        padding:12px 18px; font-weight:700; cursor:pointer; color:var(--text-main); font-size:15px;
        display:flex; align-items:center; gap:8px;
    }
    .flash-controls button:hover { background:var(--bg-gray); }
    .flash-controls button:disabled { opacity:.4; cursor:default; }
    .flash-progress { font-weight:700; color:var(--text-secondary); font-size:14px; }
    .known-btn { background:var(--green) !important; color:#fff !important; }
    .review-btn { background:var(--amber) !important; color:#fff !important; }

    /* QUIZ MODE */
    .quiz-card { background:#fff; border-radius:12px; box-shadow:var(--card-shadow); padding:20px; margin-bottom:16px; }
    .quiz-meta { display:flex; justify-content:space-between; align-items:center; margin-bottom:16px; }
    .quiz-counter { font-weight:700; color:var(--text-secondary); }
    .quiz-bar { height:8px; background:var(--bg-gray); border-radius:6px; overflow:hidden; flex:1; margin-left:14px; }
    .quiz-bar-fill { height:100%; background:var(--meta-blue); transition:width .3s; border-radius:6px; }
    .quiz-q { font-size:20px; font-weight:700; line-height:1.4; margin-bottom:20px; }
    .quiz-opt {
        display:block; width:100%; text-align:left; background:var(--bg-gray); border:2px solid transparent;
        border-radius:10px; padding:14px 16px; margin-bottom:10px; cursor:pointer; font-size:15px;
        font-weight:600; transition:.15s;
    }
    .quiz-opt:hover:not(:disabled) { background:#e4e6e9; }
    .quiz-opt.correct { background:#e3f7e8; border-color:var(--green); color:var(--green); }
    .quiz-opt.wrong { background:#fdeaed; border-color:var(--red); color:var(--red); }
    .quiz-opt:disabled { cursor:default; }
    .quiz-explain {
        background:#fff8e6; border-left:4px solid var(--amber); padding:12px 14px; border-radius:6px;
        font-size:14px; line-height:1.5; margin-top:6px; display:none;
    }
    .quiz-explain.show { display:block; }
    .quiz-next {
        width:100%; background:var(--meta-blue); color:#fff; border:none; padding:13px; border-radius:10px;
        font-weight:700; font-size:15px; cursor:pointer; margin-top:14px; display:none;
    }
    .quiz-next.show { display:block; }
    .quiz-next:hover { background:var(--meta-blue-dark); }
    .quiz-result { text-align:center; padding:20px; }
    .quiz-score-num { font-size:56px; font-weight:800; color:var(--meta-blue); line-height:1; }
    .quiz-result h2 { margin:14px 0 6px; }
    .quiz-result p { color:var(--text-secondary); margin-bottom:20px; }
    .quiz-restart {
        background:var(--meta-blue); color:#fff; border:none; padding:13px 28px; border-radius:10px;
        font-weight:700; font-size:15px; cursor:pointer;
    }
    .quiz-setup { text-align:center; padding:10px; }
    .quiz-setup h2 { margin-bottom:6px; }
    .quiz-setup p { color:var(--text-secondary); margin-bottom:18px; font-size:14px; }
    .quiz-setup-row { display:flex; gap:8px; flex-wrap:wrap; justify-content:center; margin-bottom:18px; }
    .chip {
        padding:8px 16px; border-radius:20px; border:2px solid #ced0d4; background:#fff;
        cursor:pointer; font-weight:600; font-size:14px; transition:.15s;
    }
    .chip.selected { background:var(--meta-blue); border-color:var(--meta-blue); color:#fff; }

    /* WIDGETS */
    .widget { background:#fff; border-radius:10px; padding:16px; box-shadow:var(--card-shadow); margin-bottom:16px; }
    .widget h4 { margin-bottom:12px; font-size:15px; color:var(--text-main); display:flex; align-items:center; gap:8px; }
    .binary-tool input { width:100%; padding:10px; border:1px solid #ddd; border-radius:8px; margin-bottom:10px; font-size:15px; }
    .binary-tool button { width:100%; background:var(--meta-blue); color:#fff; border:none; padding:10px; border-radius:8px; font-weight:700; cursor:pointer; }
    .binary-tool button:hover { background:var(--meta-blue-dark); }
    .result-box { margin-top:10px; padding:12px; background:#e7f3ff; border-radius:8px; text-align:center; font-weight:700; color:var(--meta-blue); font-family:'Courier New',monospace; font-size:17px; }
    .bit-grid { display:flex; gap:4px; justify-content:center; margin-top:12px; flex-wrap:nowrap; }
    .bit { flex:1 1 0; min-width:0; text-align:center; }
    .bit .val {
        width:100%; aspect-ratio:1/1; border-radius:6px; display:flex; align-items:center; justify-content:center;
        font-weight:700; cursor:pointer; font-family:'Courier New',monospace; user-select:none; transition:.1s; font-size:15px;
    }
    .bit .val.on { background:var(--meta-blue); color:#fff; }
    .bit .val.off { background:var(--bg-gray); color:var(--text-secondary); }
    .bit .val:hover { transform:scale(1.08); }
    .bit .place { font-size:10px; color:var(--text-secondary); margin-top:4px; }

    .progress-ring { display:flex; align-items:center; gap:14px; }
    .ring-stat { font-size:13px; color:var(--text-secondary); }
    .ring-stat b { display:block; font-size:22px; color:var(--text-main); }
    .stat-row { display:flex; justify-content:space-between; font-size:13px; padding:6px 0; border-bottom:1px solid #f0f0f0; }
    .stat-row:last-child { border:none; }
    .stat-row b { color:var(--meta-blue); }

    .tip-text { font-size:14px; color:var(--text-secondary); line-height:1.5; }
    .reset-link { color:var(--red); font-size:12px; cursor:pointer; text-decoration:underline; margin-top:8px; display:inline-block; }

    .empty-state { text-align:center; padding:40px 20px; color:var(--text-secondary); }
    .empty-state i { font-size:40px; margin-bottom:12px; color:#bcc0c4; }

    @media (max-width:900px) {
        .container { grid-template-columns:1fr; }
        .sidebar, .widgets { display:none; position:static; }
        .brand-name, .nav-left .logo { display:none; }
        .search-bar { max-width:none; }
        .mode-btn { font-size:13px; padding:10px 2px; gap:5px; }
        .post-content, .post-image, .card-q, .card-a, .quiz-q { overflow-wrap:break-word; word-break:break-word; }
        .post-image { font-size:13px; padding:16px; }
    }
    @media (max-width:380px) {
        .mode-btn i { display:none; }
    }
</style>
</head>
<body>

<nav>
    <div class="nav-left">
        <i class="fas fa-graduation-cap logo"></i>
        <span class="brand-name">ictBook</span>
        <div class="search-bar">
            <i class="fas fa-search" style="color:var(--text-secondary)"></i>
            <input type="text" id="topicSearch" placeholder="Search topics..." onkeyup="filterPosts()">
        </div>
    </div>
    <div class="nav-right">
        <div class="level-badge">
            <i class="fas fa-user-graduate"></i>
            <select id="yearFilter" onchange="applyFilters()">
                <option value="all">All Years</option>
                <option value="5">Year 5 · Easy</option>
                <option value="6">Year 6 · Average</option>
                <option value="7">Year 7 · Hard</option>
            </select>
        </div>
    </div>
</nav>

<div class="container">
    <!-- LEFT SIDEBAR -->
    <div class="sidebar">
        <h3>Study Groups</h3>
        <div class="sidebar-item active-tab" data-cat="all" onclick="filterCategory('all', this)">
            <i class="fas fa-newspaper"></i> News Feed
            <span class="pillar-count" id="count-all">0</span>
        </div>
        <div class="sidebar-item" data-cat="Hardware" onclick="filterCategory('Hardware', this)">
            <i class="fas fa-microchip" style="color:#fb3958;"></i> Hardware & Systems
            <span class="pillar-count" id="count-Hardware">0</span>
        </div>
        <div class="sidebar-item" data-cat="Networks" onclick="filterCategory('Networks', this)">
            <i class="fas fa-network-wired" style="color:#42b72a;"></i> Networks & Data
            <span class="pillar-count" id="count-Networks">0</span>
        </div>
        <div class="sidebar-item" data-cat="Data" onclick="filterCategory('Data', this)">
            <i class="fas fa-calculator" style="color:#1877f2;"></i> Data Representation
            <span class="pillar-count" id="count-Data">0</span>
        </div>
        <div class="sidebar-item" data-cat="Coding" onclick="filterCategory('Coding', this)">
            <i class="fas fa-code" style="color:#ffab19;"></i> Thinking & Coding
            <span class="pillar-count" id="count-Coding">0</span>
        </div>
        <div class="sidebar-item" data-cat="Scratch" onclick="filterCategory('Scratch', this)">
            <i class="fas fa-cat" style="color:#ff8c1a;"></i> Scratch Lab
            <span class="pillar-count" id="count-Scratch">0</span>
        </div>
        <div class="sidebar-item" data-cat="Safety" onclick="filterCategory('Safety', this)">
            <i class="fas fa-shield-halved" style="color:#8a3ffc;"></i> Privacy & Safety
            <span class="pillar-count" id="count-Safety">0</span>
        </div>
        <div class="sidebar-divider"></div>
        <div class="sidebar-item" data-cat="saved" onclick="filterCategory('saved', this)">
            <i class="fas fa-bookmark" style="color:var(--amber);"></i> Saved Cards
            <span class="pillar-count" id="count-saved">0</span>
        </div>
    </div>

    <!-- CENTER -->
    <div class="main-content">
        <!-- MODE SWITCH -->
        <div class="mode-switch">
            <button class="mode-btn active" data-mode="feed" onclick="switchMode('feed')"><i class="fas fa-stream"></i> Feed</button>
            <button class="mode-btn" data-mode="flashcards" onclick="switchMode('flashcards')"><i class="fas fa-clone"></i> Flashcards</button>
            <button class="mode-btn" data-mode="quiz" onclick="switchMode('quiz')"><i class="fas fa-trophy"></i> Quiz Bee</button>
        </div>

        <!-- FEED MODE -->
        <div id="feedMode">
            <div class="stories-container" id="storiesBar"></div>
            <div id="feedContent"></div>
        </div>

        <!-- FLASHCARD MODE -->
        <div id="flashcardMode" style="display:none;">
            <div class="flashcard-wrap">
                <div class="flashcard" id="flashcard" onclick="flipCard()">
                    <div class="card-face card-front">
                        <div class="card-eyebrow" id="fcEyebrow">Hardware · Year 5</div>
                        <div class="card-q" id="fcQuestion">Question</div>
                        <div class="card-hint"><i class="fas fa-hand-pointer"></i> Tap to reveal answer</div>
                    </div>
                    <div class="card-face card-back">
                        <div class="card-eyebrow">Answer</div>
                        <div class="card-a" id="fcAnswer">Answer</div>
                        <div class="card-hint"><i class="fas fa-hand-pointer"></i> Tap to flip back</div>
                    </div>
                </div>
            </div>
            <div class="flash-controls">
                <button onclick="prevCard()" id="fcPrev"><i class="fas fa-chevron-left"></i> Back</button>
                <span class="flash-progress" id="fcProgress">1 / 1</span>
                <button onclick="nextCard()" id="fcNext">Next <i class="fas fa-chevron-right"></i></button>
            </div>
            <div class="flash-controls" style="margin-top:10px;">
                <button class="review-btn" onclick="markCard('review')" style="flex:1;"><i class="fas fa-rotate"></i> Review again</button>
                <button class="known-btn" onclick="markCard('known')" style="flex:1;"><i class="fas fa-check"></i> Got it</button>
            </div>
        </div>

        <!-- QUIZ MODE -->
        <div id="quizMode" style="display:none;">
            <div class="quiz-card" id="quizContainer"></div>
        </div>
    </div>

    <!-- RIGHT WIDGETS -->
    <div class="widgets">
        <div class="widget">
            <h4><i class="fas fa-calculator" style="color:var(--meta-blue)"></i> Binary ⟷ Decimal</h4>
            <div class="binary-tool">
                <input type="number" id="decInput" placeholder="Decimal (0–255)" oninput="liveConvert()" min="0" max="255">
                <button onclick="runConversion()">Convert</button>
                <div id="binResult" class="result-box"><span style="font-size:18px">00000000</span><br><span style="font-size:13px;color:var(--text-secondary);font-weight:600">= 0 in decimal</span></div>
                <div class="bit-grid" id="bitGrid"></div>
                <p style="font-size:12px; color:var(--text-secondary); text-align:center; margin-top:8px;">Tap the bits to build a number!</p>
            </div>
        </div>

        <div class="widget">
            <h4><i class="fas fa-chart-line" style="color:var(--green)"></i> My Progress</h4>
            <div class="stat-row"><span>Cards mastered</span><b id="statMastered">0</b></div>
            <div class="stat-row"><span>Cards to review</span><b id="statReview">0</b></div>
            <div class="stat-row"><span>Best quiz score</span><b id="statBest">—</b></div>
            <div class="stat-row"><span>Quizzes taken</span><b id="statQuizzes">0</b></div>
            <span class="reset-link" onclick="resetProgress()">Reset all progress</span>
        </div>

        <div class="widget">
            <h4><i class="fas fa-lightbulb" style="color:var(--amber)"></i> Did you know?</h4>
            <p class="tip-text" id="tipText"></p>
        </div>
    </div>
</div>

<script>
/* ============================================================
   QUESTION BANK — all 5 pillars, Years 5–7
   pillar = sidebar group, year = 5/6/7, author/color = poster identity
   term/def power the flashcards; quiz = MCQ for Quiz Bee
   ============================================================ */
const CARDS = [
  // ---------- HARDWARE & DIGITAL SYSTEMS ----------
  { cat:"Hardware", year:5, author:"System Admin", color:"#fb3958",
    term:"What is the CPU?",
    def:"The CPU (Central Processing Unit) is the BRAIN of the computer. It follows instructions and processes all the data.",
    content:"The <b>CPU (Central Processing Unit)</b> is the brain of the computer. It follows instructions and processes data. Without me, nothing happens!",
    visual:"[ INPUT ] → [ CPU ] → [ OUTPUT ]",
    quiz:{ q:"Which part is known as the 'brain' of the computer?", opts:["RAM","CPU","Monitor","Mouse"], answer:1,
           why:"The CPU (Central Processing Unit) processes instructions — it's the brain." } },

  { cat:"Hardware", year:5, author:"System Admin", color:"#fb3958",
    term:"What does RAM do?",
    def:"RAM (Random Access Memory) is TEMPORARY memory. It holds the data and programs you're using right now. It is wiped when you switch off.",
    content:"<b>RAM</b> is temporary memory. It holds whatever you're working on right now — but it forgets everything when the power goes off! 💨",
    visual:"RAM = short-term memory",
    quiz:{ q:"What happens to data in RAM when the computer is switched off?", opts:["It is saved forever","It is lost","It moves to the CPU","It prints out"], answer:1,
           why:"RAM is temporary (volatile) memory, so its contents are lost when power is off." } },

  { cat:"Hardware", year:6, author:"System Admin", color:"#fb3958",
    term:"Input vs Output devices?",
    def:"INPUT devices send data INTO the computer (keyboard, mouse, mic). OUTPUT devices send data OUT to the user (monitor, printer, speakers).",
    content:"The <b>System Model</b>: Input devices (keyboard, mouse) feed data IN. The CPU Processes it. Output devices (monitor, printer) send results OUT.",
    visual:"INPUT → PROCESS → OUTPUT",
    quiz:{ q:"Which of these is an OUTPUT device?", opts:["Keyboard","Microphone","Printer","Scanner"], answer:2,
           why:"A printer sends information OUT to the user, so it's an output device." } },

  { cat:"Hardware", year:7, author:"System Admin", color:"#fb3958",
    term:"HDD vs SSD?",
    def:"HDD (Hard Disk Drive) uses spinning magnetic disks — mechanical, moving parts, cheaper, slower. SSD (Solid State Drive) uses flash memory — no moving parts, much faster, more durable.",
    content:"<b>HDD vs SSD</b>: A Hard Disk Drive has spinning disks and moving parts (slower). A Solid State Drive uses flash memory with no moving parts (much faster!). ⚡",
    visual:"HDD = spinning disk · SSD = flash chip",
    quiz:{ q:"What is the main advantage of an SSD over an HDD?", opts:["It is cheaper","It has moving parts","It is faster with no moving parts","It stores less data"], answer:2,
           why:"An SSD uses flash memory with no moving parts, making it faster and more durable." } },

  { cat:"Hardware", year:7, author:"System Admin", color:"#fb3958",
    term:"What is the Motherboard?",
    def:"The Motherboard is the main circuit board that CONNECTS all the components — the CPU, RAM, and storage all plug into it so they can communicate.",
    content:"The <b>Motherboard</b> is the connector — the main board where the CPU, RAM and storage all plug in and talk to each other.",
    visual:"Everything plugs into the Motherboard",
    quiz:{ q:"What is the job of the motherboard?", opts:["To store files permanently","To connect all the components together","To process instructions","To display images"], answer:1,
           why:"The motherboard is the main circuit board connecting all parts so they can communicate." } },

  // ---------- NETWORKS & DATA TRANSMISSION ----------
  { cat:"Networks", year:5, author:"Net Ranger", color:"#42b72a",
    term:"What is Wi-Fi?",
    def:"Wi-Fi is a WIRELESS way to connect devices to a network and the internet using radio waves — no cables needed!",
    content:"<b>Wi-Fi</b> connects your devices to the internet wirelessly using radio waves — no cables required! 📶",
    visual:"📱 ))) 📶 ((( 🖥️",
    quiz:{ q:"How does Wi-Fi send data between devices?", opts:["Through cables","Using radio waves","Through the CPU","Using electricity in copper"], answer:1,
           why:"Wi-Fi is wireless — it carries data using radio waves." } },

  { cat:"Networks", year:5, author:"Net Ranger", color:"#42b72a",
    term:"LAN vs WAN?",
    def:"A LAN (Local Area Network) covers a SMALL area like a school or home. A WAN (Wide Area Network) covers a HUGE area — the Internet is the biggest WAN.",
    content:"<b>LAN vs WAN</b>: A LAN (Local Area Network) is small — like your school. A WAN (Wide Area Network) is huge — the Internet itself!",
    visual:"LAN = school · WAN = the Internet",
    quiz:{ q:"The Internet is the best example of which type of network?", opts:["LAN","WAN","CPU","RAM"], answer:1,
           why:"The Internet is a WAN — a Wide Area Network spanning the whole world." } },

  { cat:"Networks", year:6, author:"Net Ranger", color:"#42b72a",
    term:"What is a Packet?",
    def:"A Packet is a small PIECE of data. Big files are broken into many packets, sent across the network, and reassembled in the right order at the other end.",
    content:"What is a <b>Packet</b>? 📦 Data is broken into small pieces called packets, sent separately across the network, then put back together at the other end!",
    visual:"FILE → [📦][📦][📦] → FILE",
    quiz:{ q:"Why is data broken into packets before sending?", opts:["To make it bigger","So small pieces can travel and be reassembled","To delete it","To turn it into binary"], answer:1,
           why:"Splitting data into packets lets pieces travel the network and be reassembled at the destination." } },

  { cat:"Networks", year:6, author:"Net Ranger", color:"#42b72a",
    term:"Wired vs Wireless?",
    def:"WIRED uses physical cables (Ethernet, fibre-optic) — usually faster and more stable. WIRELESS uses radio signals (Wi-Fi, Bluetooth) — more convenient but can be slower.",
    content:"<b>Wired vs Wireless</b>: Wired (Ethernet/fibre) is fast and stable. Wireless (Wi-Fi/Bluetooth) is convenient but signal can drop.",
    visual:"🔌 Wired  vs  📶 Wireless",
    quiz:{ q:"Which connection uses Bluetooth?", opts:["Wired","Wireless","Fibre-optic","Ethernet"], answer:1,
           why:"Bluetooth is a wireless technology — it uses radio signals, not cables." } },

  { cat:"Networks", year:7, author:"Net Ranger", color:"#42b72a",
    term:"What is a NIC?",
    def:"A NIC (Network Interface Card) is the hardware inside a device that lets it CONNECT to a network — it gives the device its network identity.",
    content:"A <b>NIC (Network Interface Card)</b> is the hardware that connects a device to a network. Routers direct traffic, Switches link devices, Modems link you to your ISP.",
    visual:"NIC · Router · Switch · Modem",
    quiz:{ q:"What does a NIC (Network Interface Card) do?", opts:["Stores files","Connects a device to a network","Processes data","Displays graphics"], answer:1,
           why:"A NIC is the hardware that lets a device connect to and communicate on a network." } },

  { cat:"Networks", year:7, author:"Net Ranger", color:"#42b72a",
    term:"Copper vs Fibre-optic?",
    def:"Copper cables carry data as ELECTRICITY. Fibre-optic cables carry data as pulses of LIGHT — much faster and over longer distances.",
    content:"<b>Transmission media</b>: Copper cables send data using electricity. Fibre-optic cables send it as light — faster and over longer distances! 💡",
    visual:"Copper = electricity · Fibre = light",
    quiz:{ q:"How does a fibre-optic cable carry data?", opts:["As electricity","As radio waves","As pulses of light","As packets of paper"], answer:2,
           why:"Fibre-optic cables transmit data as pulses of light, which is very fast." } },

  // ---------- DATA REPRESENTATION ----------
  { cat:"Data", year:5, author:"Data Detective", color:"#1877f2",
    term:"What digits are used in Binary?",
    def:"Binary uses only TWO digits: 0 and 1. 0 means OFF and 1 means ON. Every piece of computer data is made from these.",
    content:"Computers talk in <b>Binary</b> — only 0s (Off) and 1s (On). Every pixel, letter and sound on your screen is just binary code underneath!",
    visual:"0 = OFF   1 = ON",
    quiz:{ q:"How many different digits does binary use?", opts:["10","2","8","16"], answer:1,
           why:"Binary uses just two digits: 0 and 1." } },

  { cat:"Data", year:6, author:"Data Detective", color:"#1877f2",
    term:"Convert 1010 to decimal",
    def:"1010 = 10. Using place values 8-4-2-1: (1×8)+(0×4)+(1×2)+(0×1) = 8+2 = 10.",
    content:"Convert <b>1010</b> to decimal! Place values are 8-4-2-1. So (1×8)+(0×4)+(1×2)+(0×1) = 8+2 = <b>10</b>. ✅",
    visual:"8 4 2 1 → 1010 = 8+2 = 10",
    quiz:{ q:"What is binary 1010 in decimal?", opts:["8","10","12","1010"], answer:1,
           why:"Place values 8-4-2-1: (1×8)+(1×2) = 10. Try it in the converter on the right!" } },

  { cat:"Data", year:6, author:"Data Detective", color:"#1877f2",
    term:"How are images stored?",
    def:"Images are made of PIXELS — tiny squares of colour. Each pixel's colour is stored as a binary number, so the whole image is just numbers to the computer.",
    content:"Every image is a grid of <b>Pixels</b> — tiny coloured squares. Each pixel's colour is stored as a binary number. Zoom in far enough and you'll see them! 🟦🟥🟩",
    visual:"🖼️ = grid of coloured pixels",
    quiz:{ q:"What is a pixel?", opts:["A type of cable","A tiny square of colour in an image","A network device","A binary digit"], answer:1,
           why:"A pixel is one tiny square of colour; many pixels together make an image." } },

  { cat:"Data", year:7, author:"Data Detective", color:"#1877f2",
    term:"How is sound stored digitally?",
    def:"Sound is recorded by taking many tiny SAMPLES of a sound wave each second. Each sample is saved as a binary number. More samples = better quality but bigger files.",
    content:"<b>Digital sound</b> 🎵 is made by taking thousands of tiny samples of a sound wave every second. Each sample is stored as a binary number — more samples means clearer sound!",
    visual:"〰️ wave → [samples] → binary",
    quiz:{ q:"How is sound turned into something a computer can store?", opts:["By drawing pixels","By taking samples of the sound wave","By splitting it into packets","By cooling the CPU"], answer:1,
           why:"Sound is sampled many times a second, and each sample is saved as a binary number." } },

  { cat:"Data", year:7, author:"Data Detective", color:"#1877f2",
    term:"What units measure file size?",
    def:"A single 0 or 1 is a BIT. 8 bits make 1 BYTE. About 1,000 bytes = 1 kilobyte (KB), 1,000 KB = 1 megabyte (MB), 1,000 MB = 1 gigabyte (GB).",
    content:"<b>File size units</b>: 1 bit (a 0 or 1) → 8 bits = 1 byte → 1,000 bytes ≈ 1 KB → 1,000 KB ≈ 1 MB → 1,000 MB ≈ 1 GB. Each step is about a thousand times bigger! 📦",
    visual:"bit → Byte → KB → MB → GB",
    quiz:{ q:"How many bits are in one byte?", opts:["2","4","8","16"], answer:2,
           why:"There are 8 bits in 1 byte — the basic unit for measuring file size." } },

  // ---------- COMPUTATIONAL THINKING & PROGRAMMING ----------
  { cat:"Coding", year:5, author:"Scratch Master", color:"#ffab19",
    term:"Flowchart Start/End symbol?",
    def:"The Start and End of a flowchart use a ROUNDED rectangle (an oval / 'terminator'). Diamonds are for decisions, rectangles for processes.",
    content:"In a <b>Flowchart</b>, the Start and End are shown with a rounded oval (terminator). 💎 Diamonds = decisions, ▭ Rectangles = actions.",
    visual:"( Start ) ▢ Process ◇ Decision ( End )",
    quiz:{ q:"Which flowchart shape shows the Start or End?", opts:["Diamond","Rounded oval","Rectangle","Arrow"], answer:1,
           why:"Start/End uses a rounded oval (terminator). Diamonds are decisions." } },

  { cat:"Coding", year:6, author:"Scratch Master", color:"#ffab19",
    term:"What is a Variable?",
    def:"A Variable is a named BOX that stores information which can change — like a Score, a Timer, or a player's name in your game.",
    content:"What is a <b>Variable</b>? 📦 It's a container that stores information that can change — like your Score or a Timer in a game!",
    visual:"SET [Score] TO (Score + 1)",
    quiz:{ q:"What is a variable used for in programming?", opts:["Connecting to Wi-Fi","Storing information that can change","Drawing pixels","Cooling the CPU"], answer:1,
           why:"A variable is a named store for a value that can change as the program runs." } },

  { cat:"Coding", year:6, author:"CT Pillars", color:"#ffab19",
    term:"The 4 pillars of Computational Thinking?",
    def:"1) Decomposition – break it down. 2) Pattern recognition – find similarities. 3) Abstraction – ignore the unimportant details. 4) Algorithmic thinking – designing step-by-step rules.",
    content:"The <b>4 pillars of Computational Thinking</b>:<br>1. <b>Decomposition</b> (break it down)<br>2. <b>Patterns</b> (find similarities)<br>3. <b>Abstraction</b> (ignore the fluff)<br>4. <b>Algorithmic thinking</b> (step-by-step rules)",
    visual:"Decompose · Pattern · Abstract · Algorithmic thinking",
    quiz:{ q:"Which pillar means breaking a big problem into smaller parts?", opts:["Abstraction","Decomposition","Iteration","Pattern recognition"], answer:1,
           why:"Decomposition = breaking a problem down into smaller, manageable parts." } },

  { cat:"Coding", year:7, author:"Scratch Master", color:"#ffab19",
    term:"Sequence, Selection, Iteration?",
    def:"SEQUENCE = steps in order. SELECTION = making a choice (If–Then–Else / branching). ITERATION = repeating steps (loops, 'repeat until').",
    content:"Three programming constructs: <b>Sequence</b> (steps in order), <b>Selection</b> (If-Then-Else choices), and <b>Iteration</b> (loops that repeat). 🔁",
    visual:"IF score > 10 THEN win ELSE play",
    quiz:{ q:"A loop that repeats code is an example of…", opts:["Sequence","Selection","Iteration","Decomposition"], answer:2,
           why:"Iteration means repetition — loops like 'repeat until' are iteration." } },

  // ---------- SCRATCH LAB (block-based programming) ----------
  { cat:"Scratch", year:5, author:"Scratch Master", color:"#ff8c1a",
    term:"What is a Sprite in Scratch?",
    def:"A Sprite is a character or object you can program in Scratch — like the cat. You give each sprite blocks of code to make it move, talk, or react.",
    content:"In Scratch, a <b>Sprite</b> 🐱 is a character or object you control with code. The orange cat is the famous default sprite — you can add your own too!",
    visual:"🐱 Sprite = a character you code",
    quiz:{ q:"What is a sprite in Scratch?", opts:["A type of loop","A character or object you program","A error message","A network device"], answer:1,
           why:"A sprite is a character or object on the stage that you give code to." } },

  { cat:"Scratch", year:5, author:"Scratch Master", color:"#ff8c1a",
    term:"What does the green flag do?",
    def:"Clicking the green flag STARTS your Scratch program. Blocks placed under 'when green flag clicked' run from the top down when you press it.",
    content:"The <b>green flag</b> 🚩 starts your project! Put your code under the 'when 🚩 clicked' block and it runs the moment you press play. The red stop sign ends it.",
    visual:"when 🚩 clicked → run code",
    quiz:{ q:"What happens when you click the green flag in Scratch?", opts:["It deletes your project","It starts the program running","It adds a new sprite","It saves the file"], answer:1,
           why:"The green flag starts the program — code under 'when green flag clicked' runs." } },

  { cat:"Scratch", year:6, author:"Scratch Master", color:"#ff8c1a",
    term:"What are the Scratch block colours?",
    def:"Scratch blocks are colour-coded by job: blue = Motion, purple = Looks, pink = Sound, yellow = Events, gold/orange = Control (loops & ifs), light blue = Sensing.",
    content:"Scratch <b>blocks are colour-coded</b> by what they do:<br>🔵 Motion (move/turn)<br>🟣 Looks (say/show)<br>🟡 Events (when clicked)<br>🟠 Control (loops & if)",
    visual:"🔵 Motion 🟣 Looks 🟡 Events 🟠 Control",
    quiz:{ q:"In Scratch, which colour blocks make a sprite move?", opts:["Blue (Motion)","Purple (Looks)","Pink (Sound)","Yellow (Events)"], answer:0,
           why:"Blue Motion blocks (like 'move 10 steps') control how a sprite moves." } },

  { cat:"Scratch", year:6, author:"Scratch Master", color:"#ff8c1a",
    term:"What is a 'forever' loop?",
    def:"A 'forever' block repeats the blocks inside it again and again, without stopping. It's Scratch's way of doing iteration — great for things that always need checking.",
    content:"The <b>forever</b> block 🔁 repeats the code inside it endlessly. Perfect for a sprite that should always move, or always check if it touches something!",
    visual:"forever { move · check · repeat }",
    quiz:{ q:"What does a 'forever' block do in Scratch?", opts:["Runs code once","Repeats code endlessly","Stops the program","Adds a sprite"], answer:1,
           why:"'forever' is a loop (iteration) — it repeats the blocks inside without stopping." } },

  { cat:"Scratch", year:7, author:"Scratch Master", color:"#ff8c1a",
    term:"How does an 'if-then' block work?",
    def:"An 'if-then' block is selection: it checks a condition and only runs the blocks inside IF the condition is true. e.g. if 'touching edge' then 'bounce'.",
    content:"The <b>if-then</b> block 🔶 is a decision. It runs the code inside only when its condition is true — like 'if touching edge, then turn around'.",
    visual:"if <touching edge?> then [bounce]",
    quiz:{ q:"An 'if-then' block in Scratch is an example of which construct?", opts:["Sequence","Selection","Iteration","A variable"], answer:1,
           why:"'if-then' makes a choice based on a condition — that's selection." } },

  { cat:"Scratch", year:7, author:"Scratch Master", color:"#ff8c1a",
    term:"What are events & broadcasts?",
    def:"Events start scripts (e.g. 'when green flag clicked', 'when space key pressed'). A 'broadcast' sends a hidden message so one sprite can trigger another sprite's code.",
    content:"<b>Events</b> 🟡 trigger code — like 'when key pressed'. A <b>broadcast</b> 📣 sends a message so one sprite can start another sprite's script. Great for teamwork between sprites!",
    visual:"broadcast [start] → when I receive [start]",
    quiz:{ q:"What is a 'broadcast' used for in Scratch?", opts:["To delete a sprite","To send a message that triggers other code","To change a colour","To save the project"], answer:1,
           why:"A broadcast sends a message so one sprite can trigger another sprite's script." } },

  // ---------- PRIVACY, SECURITY & ETHICS ----------
  { cat:"Safety", year:5, author:"Cyber Guard", color:"#8a3ffc",
    term:"Personal vs Private info?",
    def:"PERSONAL info (favourite colour, hobby) is usually safe to share. PRIVATE info (home address, passwords, phone number) is dangerous to share online.",
    content:"Is it <b>Personal</b> or <b>Private</b>? Personal info (favourite colour) is okay to share. Private info (home address, passwords) is NOT safe! 🔒",
    visual:"🔒 PROTECT YOUR PRIVATE DATA",
    quiz:{ q:"Which of these is PRIVATE info you should NOT share online?", opts:["Favourite colour","Your home address","Favourite sport","Your hobby"], answer:1,
           why:"Your home address is private — sharing it online can be dangerous." } },

  { cat:"Safety", year:6, author:"Cyber Guard", color:"#8a3ffc",
    term:"What is netiquette?",
    def:"Netiquette means good MANNERS online: being kind and respectful, thinking before you post, not typing in ALL CAPS (it looks like shouting), and treating people as you would face to face.",
    content:"<b>Netiquette</b> 😊 is good manners on the internet — be kind, think before you post, and don't TYPE IN CAPS (it reads like shouting!). Treat people online like you would in person.",
    visual:"Be kind · Think first · No SHOUTING",
    quiz:{ q:"What does good 'netiquette' mean?", opts:["Typing as fast as possible","Being kind and respectful online","Sharing your password","Using lots of capitals"], answer:1,
           why:"Netiquette is online etiquette — being kind, respectful and thoughtful to others." } },

  { cat:"Safety", year:7, author:"Cyber Guard", color:"#8a3ffc",
    term:"What is reliable information?",
    def:"Reliable info is trustworthy and accurate. Check who wrote it, when, and whether other trusted sites agree. Not everything online is true — always cross-check before believing or sharing.",
    content:"Is it <b>reliable</b>? 🔎 Before trusting info online, check WHO wrote it, WHEN, and if other trusted sites agree. Not everything on the internet is true!",
    visual:"Who? · When? · Cross-check ✅",
    quiz:{ q:"How can you check if online information is reliable?", opts:["Believe the first result","Check the source and cross-check it","Share it straight away","Only read the headline"], answer:1,
           why:"Reliable info comes from checking the source, the date, and cross-checking other trusted sites." } },
];

const TIPS = [
  "Binary 11111111 = 255, the biggest number you can make with 8 bits (one byte).",
  "The word 'bit' is short for Binary digIT — a single 0 or 1.",
  "8 bits = 1 byte. 1024 bytes = 1 kilobyte!",
  "The first computer 'bug' was a real moth stuck in a machine in 1947.",
  "Fibre-optic cables can carry data at almost the speed of light.",
  "A QR code is just binary stored as a pattern of black and white squares.",
  "Scratch was made at MIT to help young people learn to code with blocks.",
  "Your fingerprint is so unique that no two people have ever matched."
];

/* ============================================================
   STATE (saved in memory only — resets on reload, no storage APIs)
   ============================================================ */
let state = {
  mode: "feed",
  category: "all",
  year: "all",
  search: "",
  liked: new Set(),
  saved: new Set(),
  known: new Set(),
  review: new Set(),
  bestScore: null,
  quizzesTaken: 0,
  fcIndex: 0,
  fcDeck: [],
};
const cardId = c => c.cat + "|" + c.term;

/* ============================================================
   FILTERING
   ============================================================ */
function getFiltered() {
  return CARDS.filter(c => {
    if (state.category === "saved") { if (!state.saved.has(cardId(c))) return false; }
    else if (state.category !== "all" && c.cat !== state.category) return false;
    if (state.year !== "all" && String(c.year) !== state.year) return false;
    if (state.search) {
      const q = state.search.toLowerCase();
      if (!(c.content.toLowerCase().includes(q) || c.term.toLowerCase().includes(q) ||
            c.def.toLowerCase().includes(q) || c.cat.toLowerCase().includes(q))) return false;
    }
    return true;
  });
}

/* ============================================================
   FEED MODE
   ============================================================ */
const yearColor = y => y===5 ? "#42b72a" : y===6 ? "#f7b928" : "#e41e3f";
const yearLabel = y => y===5 ? "Y5 · Easy" : y===6 ? "Y6 · Avg" : "Y7 · Hard";

function renderFeed() {
  const feed = document.getElementById("feedContent");
  const data = getFiltered();
  if (!data.length) {
    feed.innerHTML = `<div class="post empty-state">
      <i class="fas fa-folder-open"></i>
      <p><b>No cards here yet.</b><br>Try another group, year level, or clear your search.</p>
    </div>`;
    return;
  }
  feed.innerHTML = data.map(c => {
    const id = cardId(c);
    const liked = state.liked.has(id);
    const saved = state.saved.has(id);
    return `
      <div class="post">
        <div class="post-header">
          <div class="post-avatar" style="background:${c.color}">${c.author[0]}</div>
          <div class="post-info">
            <b>${c.author}</b>
            <small>${c.cat} Group · <span class="yr-tag" style="background:${yearColor(c.year)}">${yearLabel(c.year)}</span></small>
          </div>
        </div>
        <div class="post-content">${c.content}</div>
        <div class="post-image">${c.visual}</div>
        <div class="post-actions">
          <div class="action-btn ${liked?'liked':''}" onclick="toggleLike('${id}',this)">
            <i class="fa-thumbs-up ${liked?'fas':'far'}"></i> ${liked?'Liked':'Like'}
          </div>
          <div class="action-btn" onclick="focusComment(this)"><i class="far fa-comment"></i> Comment</div>
          <div class="action-btn ${saved?'saved':''}" onclick="toggleSave('${id}',this)">
            <i class="fa-bookmark ${saved?'fas':'far'}"></i> ${saved?'Saved':'Save'}
          </div>
        </div>
        <div class="comment-section">
          <div class="comment"><b>Study Tip 💡:</b> ${c.def}</div>
        </div>
        <div class="comment-input">
          <div class="post-avatar" style="width:30px;height:30px;font-size:12px;background:var(--meta-blue)">Me</div>
          <input type="text" placeholder="Write a note to yourself…" onkeypress="addComment(event,this)">
        </div>
      </div>`;
  }).join("");
}

function toggleLike(id, btn) {
  if (state.liked.has(id)) state.liked.delete(id); else state.liked.add(id);
  const liked = state.liked.has(id);
  btn.classList.toggle("liked", liked);
  btn.innerHTML = `<i class="fa-thumbs-up ${liked?'fas':'far'}"></i> ${liked?'Liked':'Like'}`;
}
function toggleSave(id, btn) {
  if (state.saved.has(id)) state.saved.delete(id); else state.saved.add(id);
  const saved = state.saved.has(id);
  btn.classList.toggle("saved", saved);
  btn.innerHTML = `<i class="fa-bookmark ${saved?'fas':'far'}"></i> ${saved?'Saved':'Save'}`;
  updateCounts(); updateProgress();
  if (state.category === "saved") renderFeed();
}
function focusComment(btn) {
  btn.closest(".post").querySelector(".comment-input input").focus();
}
function addComment(e, input) {
  if (e.key === "Enter" && input.value.trim() !== "") {
    const sec = input.closest(".post").querySelector(".comment-section");
    const d = document.createElement("div");
    d.className = "comment";
    d.innerHTML = `<b>You:</b> ${input.value.replace(/</g,"&lt;")}`;
    sec.appendChild(d);
    input.value = "";
  }
}

/* ============================================================
   STORIES BAR (quick facts)
   ============================================================ */
function renderStories() {
  const facts = [
    { icon:"fa-microchip", bg:"#fb3958", name:"CPU = Brain", cat:"Hardware" },
    { icon:"fa-box", bg:"#42b72a", name:"Data = Packets", cat:"Networks" },
    { icon:"fa-1", bg:"#1877f2", name:"Binary 0/1", cat:"Data" },
    { icon:"fa-puzzle-piece", bg:"#ffab19", name:"4 CT Pillars", cat:"Coding" },
    { icon:"fa-cat", bg:"#ff8c1a", name:"Scratch Cat", cat:"Scratch" },
    { icon:"fa-shield-halved", bg:"#8a3ffc", name:"Stay Safe", cat:"Safety" },
  ];
  document.getElementById("storiesBar").innerHTML = facts.map(f => `
    <div class="story-card" onclick="jumpToGroup('${f.cat}')">
      <div class="fact-icon" style="background:${f.bg}"><i class="fas ${f.icon}"></i></div>
      <div class="story-name">${f.name}</div>
    </div>`).join("");
}
function jumpToGroup(cat) {
  const el = document.querySelector(`.sidebar-item[data-cat="${cat}"]`);
  if (el) filterCategory(cat, el);
  window.scrollTo({ top:0, behavior:"smooth" });
}

/* ============================================================
   FLASHCARD MODE
   ============================================================ */
function buildDeck() {
  state.fcDeck = getFiltered();
  state.fcIndex = 0;
}
function renderFlashcard() {
  const card = document.getElementById("flashcard");
  // Reset to front without an animated flip-back (avoids showing the answer briefly)
  if (card.classList.contains("flipped")) {
    card.style.transition = "none";
    card.classList.remove("flipped");
    void card.offsetWidth; // force reflow so the transition removal takes effect
    card.style.transition = "";
  }
  if (!state.fcDeck.length) {
    document.getElementById("fcEyebrow").textContent = "No cards";
    document.getElementById("fcQuestion").textContent = "Pick another group or year to study.";
    document.getElementById("fcAnswer").textContent = "—";
    document.getElementById("fcProgress").textContent = "0 / 0";
    return;
  }
  const c = state.fcDeck[state.fcIndex];
  const mastered = state.known.has(cardId(c));
  document.getElementById("fcEyebrow").innerHTML =
    `${c.cat} · ${yearLabel(c.year)} ${mastered?'· <i class="fas fa-check" style="color:#31a24c"></i> mastered':''}`;
  document.getElementById("fcQuestion").textContent = c.term;
  document.getElementById("fcAnswer").textContent = c.def;
  document.getElementById("fcProgress").textContent = `${state.fcIndex+1} / ${state.fcDeck.length}`;
  document.getElementById("fcPrev").disabled = state.fcIndex === 0;
  document.getElementById("fcNext").disabled = state.fcIndex === state.fcDeck.length-1;
}
function flipCard(){ document.getElementById("flashcard").classList.toggle("flipped"); }
function nextCard(){ if(state.fcIndex < state.fcDeck.length-1){ state.fcIndex++; renderFlashcard(); } }
function prevCard(){ if(state.fcIndex > 0){ state.fcIndex--; renderFlashcard(); } }
function markCard(type) {
  if (!state.fcDeck.length) return;
  const id = cardId(state.fcDeck[state.fcIndex]);
  if (type === "known") { state.known.add(id); state.review.delete(id); }
  else { state.review.add(id); state.known.delete(id); }
  updateProgress();
  if (state.fcIndex < state.fcDeck.length-1) { state.fcIndex++; renderFlashcard(); }
  else renderFlashcard();
}

/* ============================================================
   QUIZ MODE (Quiz Bee)
   ============================================================ */
let quiz = { questions:[], idx:0, score:0, answered:false };

function renderQuizSetup() {
  const c = document.getElementById("quizContainer");
  c.innerHTML = `
    <div class="quiz-setup">
      <h2>🏆 ICT Quiz Bee</h2>
      <p>Pick a difficulty, then answer the questions. You'll get instant feedback and a final score.</p>
      <div class="quiz-setup-row" id="quizLevelRow">
        <div class="chip selected" data-yr="all" onclick="pickChip(this)">All Years</div>
        <div class="chip" data-yr="5" onclick="pickChip(this)">Year 5</div>
        <div class="chip" data-yr="6" onclick="pickChip(this)">Year 6</div>
        <div class="chip" data-yr="7" onclick="pickChip(this)">Year 7</div>
      </div>
      <button class="quiz-restart" onclick="startQuiz()"><i class="fas fa-play"></i> Start Quiz</button>
    </div>`;
}
let chosenYear = "all";
function pickChip(el){
  document.querySelectorAll("#quizLevelRow .chip").forEach(x=>x.classList.remove("selected"));
  el.classList.add("selected");
  chosenYear = el.dataset.yr;
}
function shuffle(a){ for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]];} return a; }

function startQuiz() {
  let pool = CARDS.filter(c => chosenYear==="all" || String(c.year)===chosenYear);
  // build question objects with shuffled options
  quiz.questions = shuffle(pool.map(c => {
    const opts = c.quiz.opts.map((text,i)=>({text, correct:i===c.quiz.answer}));
    return { q:c.quiz.q, opts:shuffle(opts.slice()), why:c.quiz.why, cat:c.cat, year:c.year };
  })).slice(0, 10);
  quiz.idx = 0; quiz.score = 0; quiz.answered = false;
  renderQuizQuestion();
}

function renderQuizQuestion() {
  const c = document.getElementById("quizContainer");
  if (quiz.idx >= quiz.questions.length) { renderQuizResult(); return; }
  const Q = quiz.questions[quiz.idx];
  const pct = (quiz.idx / quiz.questions.length) * 100;
  c.innerHTML = `
    <div class="quiz-meta">
      <span class="quiz-counter">Q ${quiz.idx+1} of ${quiz.questions.length}</span>
      <div class="quiz-bar"><div class="quiz-bar-fill" style="width:${pct}%"></div></div>
    </div>
    <div class="quiz-q">${Q.q}</div>
    <div id="quizOpts">
      ${Q.opts.map((o,i)=>`<button class="quiz-opt" onclick="answerQuiz(${i})">${o.text}</button>`).join("")}
    </div>
    <div class="quiz-explain" id="quizExplain"></div>
    <button class="quiz-next" id="quizNext" onclick="nextQuiz()">
      ${quiz.idx===quiz.questions.length-1 ? 'See results <i class="fas fa-flag-checkered"></i>' : 'Next question <i class="fas fa-arrow-right"></i>'}
    </button>`;
  quiz.answered = false;
}

function answerQuiz(i) {
  if (quiz.answered) return;
  quiz.answered = true;
  const Q = quiz.questions[quiz.idx];
  const btns = document.querySelectorAll("#quizOpts .quiz-opt");
  btns.forEach((b,idx)=>{
    b.disabled = true;
    if (Q.opts[idx].correct) b.classList.add("correct");
    else if (idx===i) b.classList.add("wrong");
  });
  if (Q.opts[i].correct) quiz.score++;
  const ex = document.getElementById("quizExplain");
  ex.innerHTML = `${Q.opts[i].correct ? '✅ <b>Correct!</b> ' : '❌ <b>Not quite.</b> '} ${Q.why}`;
  ex.classList.add("show");
  document.getElementById("quizNext").classList.add("show");
}
function nextQuiz(){ quiz.idx++; renderQuizQuestion(); }

function renderQuizResult() {
  const total = quiz.questions.length;
  const pct = Math.round((quiz.score/total)*100);
  state.quizzesTaken++;
  if (state.bestScore===null || pct > state.bestScore) state.bestScore = pct;
  updateProgress();
  let msg, emoji;
  if (pct===100){ msg="Perfect score! You're quiz-bee ready! 🐝"; emoji="🏆"; }
  else if (pct>=80){ msg="Brilliant work — almost flawless!"; emoji="🌟"; }
  else if (pct>=60){ msg="Good effort! Review the tricky ones and try again."; emoji="👍"; }
  else { msg="Keep studying — flip through the flashcards and have another go!"; emoji="📚"; }
  document.getElementById("quizContainer").innerHTML = `
    <div class="quiz-result">
      <div style="font-size:46px">${emoji}</div>
      <div class="quiz-score-num">${quiz.score}/${total}</div>
      <h2>${pct}%</h2>
      <p>${msg}</p>
      <button class="quiz-restart" onclick="renderQuizSetup()"><i class="fas fa-rotate-right"></i> New Quiz</button>
    </div>`;
}

/* ============================================================
   BINARY WIDGET
   ============================================================ */
let bits = [0,0,0,0,0,0,0,0];
const PLACE = [128,64,32,16,8,4,2,1];
function showResult() {
  const dec = bits.reduce((s,b,idx)=>s + b*PLACE[idx], 0);
  document.getElementById("binResult").innerHTML =
    `<span style="font-size:18px">${bits.join("")}</span><br>` +
    `<span style="font-size:13px;color:var(--text-secondary);font-family:'Segoe UI',sans-serif;font-weight:600">= ${dec} in decimal</span>`;
}
function renderBitGrid() {
  document.getElementById("bitGrid").innerHTML = bits.map((b,i)=>`
    <div class="bit">
      <div class="val ${b?'on':'off'}" onclick="toggleBit(${i})">${b}</div>
      <div class="place">${PLACE[i]}</div>
    </div>`).join("");
}
function toggleBit(i){
  bits[i] = bits[i]?0:1;
  const dec = bits.reduce((s,b,idx)=>s + b*PLACE[idx], 0);
  document.getElementById("decInput").value = dec;
  renderBitGrid();
  showResult();
}
function setFromDecimal(dec){
  dec = Math.max(0, Math.min(255, dec||0));
  let bin = dec.toString(2).padStart(8,"0");
  bits = bin.split("").map(Number);
  renderBitGrid();
  showResult();
}
function liveConvert(){
  const v = parseInt(document.getElementById("decInput").value);
  if (!isNaN(v)) setFromDecimal(v);
}
function runConversion(){
  const v = parseInt(document.getElementById("decInput").value);
  if (isNaN(v)) return;
  setFromDecimal(v);
}

/* ============================================================
   COUNTS / PROGRESS / MODE SWITCH
   ============================================================ */
function updateCounts() {
  const cats = ["all","Hardware","Networks","Data","Coding","Scratch","Safety"];
  cats.forEach(cat=>{
    const n = cat==="all" ? CARDS.length : CARDS.filter(c=>c.cat===cat).length;
    const el = document.getElementById("count-"+cat); if(el) el.textContent = n;
  });
  document.getElementById("count-saved").textContent = state.saved.size;
}
function updateProgress() {
  document.getElementById("statMastered").textContent = state.known.size;
  document.getElementById("statReview").textContent = state.review.size;
  document.getElementById("statBest").textContent = state.bestScore===null ? "—" : state.bestScore+"%";
  document.getElementById("statQuizzes").textContent = state.quizzesTaken;
}
function resetProgress() {
  if (!confirm("Reset all likes, saves, mastered cards and quiz scores?")) return;
  state.liked.clear(); state.saved.clear(); state.known.clear(); state.review.clear();
  state.bestScore=null; state.quizzesTaken=0;
  updateCounts(); updateProgress(); applyFilters();
}

function switchMode(mode) {
  state.mode = mode;
  document.querySelectorAll(".mode-btn").forEach(b=>b.classList.toggle("active", b.dataset.mode===mode));
  document.getElementById("feedMode").style.display = mode==="feed" ? "block":"none";
  document.getElementById("flashcardMode").style.display = mode==="flashcards" ? "block":"none";
  document.getElementById("quizMode").style.display = mode==="quiz" ? "block":"none";
  if (mode==="feed") renderFeed();
  if (mode==="flashcards"){ buildDeck(); renderFlashcard(); }
  if (mode==="quiz") renderQuizSetup();
}

function filterCategory(cat, el) {
  state.category = cat;
  document.querySelectorAll(".sidebar-item").forEach(x=>x.classList.remove("active-tab"));
  el.classList.add("active-tab");
  applyFilters();
}
function applyFilters() {
  state.year = document.getElementById("yearFilter").value;
  if (state.mode==="feed") renderFeed();
  if (state.mode==="flashcards"){ buildDeck(); renderFlashcard(); }
}
function filterPosts() {
  state.search = document.getElementById("topicSearch").value;
  if (state.mode==="feed") renderFeed();
  if (state.mode==="flashcards"){ buildDeck(); renderFlashcard(); }
}

/* ============================================================
   INIT
   ============================================================ */
document.getElementById("tipText").textContent = TIPS[Math.floor(Math.random()*TIPS.length)];
renderStories();
renderFeed();
renderBitGrid();
showResult();
updateCounts();
updateProgress();
</script>
</body>
</html>
